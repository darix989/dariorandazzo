---
import { withBase } from '../lib/site';

interface Props {
	tags: string[];
}

const { tags } = Astro.props;
---

{
	tags.map((tag, index) => (
		<>
			{index > 0 && ' · '}
			<a href={withBase(`blog/tags/${tag}/`)}>{tag}</a>
		</>
	))
}
